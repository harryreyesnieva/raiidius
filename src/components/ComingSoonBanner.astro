---
export interface Props {
  year: number;
  theme?: string;
}
const { year, theme } = Astro.props;
---
<div class="rounded-lg border border-border bg-primary/5 px-4 py-3 text-sm text-foreground">
  <strong>RAIIDIUS {year} is coming soon.</strong>
  <span class="text-muted-foreground"> {theme ? `Theme: ${theme}.` : ""} Registration and submissions will open when details are announced.</span>
</div>
